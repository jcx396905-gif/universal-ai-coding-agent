# Python Todo CLI - Agent Instructions

## Environment Setup

```bash
# Set Python path
export PYTHON_SDK="/usr/bin/python3"
export PATH="$PYTHON_SDK/bin:$PATH"

# Or use init.sh
./init.sh
```

## Stack

- **Language**: Python 3.8+
- **CLI Framework**: Click
- **Database**: SQLite
- **Linting**: ruff
- **Testing**: pytest

## Quality Gates

```bash
# Install dependencies
pip install -r requirements.txt

# Lint
ruff check .

# Test
pytest

# Build/Verify
python -m py_compile **/*.py
```

## Project Structure

```
python-todo-cli/
├── main.py          # CLI entry point
├── database.py      # SQLite database
├── todos.py         # Todo model
├── requirements.txt # Dependencies
└── tests/           # Tests
```

## CLI Commands

```bash
python main.py add "Buy groceries"
python main.py list
python main.py done 1
python main.py delete 1
```

## Important Rules

1. **One task per commit** - Complete all quality gates before committing
2. **Test before marking complete** - All tests must pass
3. **Document in progress.txt** - Help future agents understand your work
4. **Stop if blocked** - Don't guess, report and wait

---

*Python Todo CLI - Generated by Universal AI Coding Agent*
