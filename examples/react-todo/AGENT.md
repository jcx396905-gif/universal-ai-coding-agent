# React Todo App - Agent Instructions

## Environment Setup

```bash
# Set Node.js path
export NODE_SDK="/usr/local/bin/node"
export PATH="$NODE_SDK/bin:$PATH"

# Or use init.sh
./init.sh
```

## Stack

- **Framework**: React 18+ with TypeScript
- **Build Tool**: Vite
- **State Management**: React Context + useLocalStorage
- **Styling**: CSS Modules
- **Linting**: ESLint
- **Testing**: Vitest + React Testing Library

## Quality Gates

```bash
# Install dependencies
npm install

# Lint
npm run lint

# Test
npm test

# Build
npm run build
```

## Project Structure

```
react-todo-app/
├── src/
│   ├── components/      # React components
│   │   ├── Header.tsx
│   │   ├── TodoList.tsx
│   │   ├── TodoItem.tsx
│   │   └── AddTodo.tsx
│   ├── hooks/           # Custom hooks
│   │   └── useLocalStorage.ts
│   ├── context/          # React Context
│   │   └── TodoContext.tsx
│   ├── types/           # TypeScript types
│   │   └── index.ts
│   ├── App.tsx          # Root component
│   └── main.tsx         # Entry point
├── index.html
├── package.json
└── vite.config.ts
```

## Available Scripts

```bash
npm run dev      # Start development server
npm run build    # Build for production
npm run preview  # Preview production build
npm run lint     # Run ESLint
npm test         # Run tests
```

## Important Rules

1. **One task per commit** - Complete all quality gates before committing
2. **Test before marking complete** - All tests must pass
3. **Device testing not required** - This is a web app, no device testing needed
4. **Document in progress.txt** - Help future agents understand your work
5. **Stop if blocked** - Don't guess, report and wait

---

*React Todo App - Generated by Universal AI Coding Agent*
